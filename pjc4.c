#include<stdio.h>
int main()
{           //if else program to finde greater number
    int a,b;
    printf("Enter the A:");
    scanf("%d",&a);
    printf("Enter the B:");
    scanf("%d",&b);
    if(a>b)
    {
        printf("A is greater than B..");
    }
    else{
        printf("B is greater than A");
    }
return 0;
}